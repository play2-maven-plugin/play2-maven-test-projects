language: java

cache:
  directories:
    - $HOME/.m2

install: "mvn install -DskipTests=true --settings .travis.settings.xml --quiet"

script: "mvn verify --settings .travis.settings.xml"

jdk:
  - oraclejdk8
